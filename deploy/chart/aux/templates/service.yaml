apiVersion: v1
kind: Service
metadata:
  name: {{ include "aux.fullname" . }}
  labels:
    {{- include "aux.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aux.selectorLabels" . | nindent 4 }}
